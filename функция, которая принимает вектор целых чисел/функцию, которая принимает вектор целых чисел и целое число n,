#include <iostream>
#include <vector>

 std::vector<int> addVec(std::vector<int>& vec, int n)
vec[i] += n;
}
return vec;
}

int main() {
std::vector<int> numbers = {1, 2, 3, 4, 5};
int n = 10;

std::vector<int> result = increaseVector(numbers, n);

for (int num : result) {
std::cout << num << " ";
}
std::cout << std::endl;

return 0;
} 
